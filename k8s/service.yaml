apiVersion: v1
kind: Service

metadata:
  name: project-1-service
  labels:
    app: project-1

spec:
  type: LoadBalancer

  selector:
    app: project-1

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
