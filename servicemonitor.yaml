apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: project-1-monitor
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: project-1
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s
